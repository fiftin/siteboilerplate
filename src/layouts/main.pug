include ../gens/components
html
    head
        block head

        title= pageTitle

        link(rel="icon" href="/img/favicon.png?_" + build_version)
        
        met(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(name="description" content=pageDescription)

        meta(property="og:title" content=pageTitle)
        meta(property="og:image:url" content="https://ansible-semaphore.com/img/banner.jpg")
        meta(property="og:image:secure_url" content="https://ansible-semaphore.com/img/banner.jpg")
        meta(property="og:image:type" content="image/jpg")
        meta(property="og:image:width" content="1793")
        meta(property="og:image:height" content="1078")
        meta(property="og:site_name" content="Ansible Semaphore")
        meta(property="og:description" content=pageDescription)
        meta(name="twitter:card" content="summary_large_image")
        meta(name="twitter:description" content=pageDescription)
        meta(name="twitter:title" content=pageTitle)
        meta(name="twitter:image" content="https://ansible-semaphore.com/img/banner.jpg")
        meta(name="twitter:site" content="@AnsibleSem")
        meta(name="twitter:creator" content="@AnsibleSem")

        //- include ../gens/fonts

        link(rel="stylesheet" href="/css/style.css?_=" + build_version)
        script(src="/js/components.js?_=" + build_version)
    body
        +SiteHeader(isHero)

        block content 

        footer
            block footer